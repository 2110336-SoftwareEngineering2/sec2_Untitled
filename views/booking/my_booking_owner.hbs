<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My booking</title>

    {{!-- jQuery --}}
    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
</head>
<body>
    {{#if results.length}}
    {{#each results}}
    <div name="booking_parent">
        <h2>Pet name : {{pet.name}}</h2>
        <h3>Date : {{formatDate startDate "DD MMMM YYYY"}} - {{formatDate endDate "DD MMMM YYYY"}}</h3>
        <h3>Pet sitter name : {{sitter.fullName}}</h3>
        <h3>Status : {{status}}</h3>
        {{#if (equals status "Requesting")}}
        <button booking-id={{id}} name="cancelBooking">Cancel</button>
        {{/if}}
        <br>
    </div>
    {{/each}}
    {{else}}
    <h1>You don't have any booking</h1>
    {{/if}}

    <script src="/javascripts/booking/myBookingOwnerHandler.js"></script>
</body>
</html>